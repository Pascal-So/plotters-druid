name: Run Tests
on:
  push: {}
  pull_request: {}
jobs:
  test:
    timeout-minutes: 30
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: cargo test --package plotters-piet
